services:
  https-proxy:
    image: nginx:alpine
    ports:
      - "127.0.0.1:8443:443"
    volumes:
      - ./ssl/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl/certs:/etc/nginx/certs:ro
    depends_on:
      - webserver
